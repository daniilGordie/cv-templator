import { Routes } from "@angular/router";
import {ResumeConstructorPage} from './resume-constructor.page'

export const resumeConstructorRoutes: Routes = [
  { path: 'resume-constructor', component: ResumeConstructorPage }
]
